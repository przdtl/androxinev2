# Androxine v2

**Telegram-first трекер тренировок.**
Минималистичный инструмент для фиксации подходов без лишней логики, экранов «начала тренировки» и сложных расписаний.

Androxine v2 — это инженерный проект, построенный на принципах Clean Architecture.
Бот и REST API используют одну кодовую базу и вызывают use cases напрямую.

---

## 📌 Философия

* Telegram — основной интерфейс
* Подход (set) — атомарная сущность
* Пользователь полностью управляет своими упражнениями
* Минимум автоматизации
* Простой шаблон тренировок (по дню недели)
* Чистая архитектура без фреймворк-логики в бизнес-слое

---

## 🧱 Архитектура

### Основная идея

* Бот и API — это адаптеры
* Вся бизнес-логика находится в `use_cases`
* HTTP не используется внутри системы
* Бот и API напрямую вызывают use cases

---

## 🗂 Доменная модель

### User

Telegram-пользователь системы.

### Category

Справочник категорий упражнений.

### Exercise

Упражнение создаётся **пользователем вручную**.
Никаких предустановленных базовых упражнений.

* name
* short (уникальный код)
* category
* is_archived

### Set

Атомарный подход.

* exercise
* weight
* reps
* performed_at

### WorkoutTemplate

Шаблон тренировки.

* name
* day_of_week (0–6)

### TemplateExercise

Связь упражнения с шаблоном.

---

## 🤖 Telegram-бот

### Формат добавления подхода

```
<short> <weight>x<reps>
```

Примеры:

```
b 100x6
sq 140x5
dl 180x3
```

Бот:

1. Определяет пользователя по telegram_id
2. Находит упражнение по short
3. Создаёт подход
4. Подтверждает запись

### Команды

* `/today` — тренировка на сегодня
* `/exercises` — список short-кодов

---

## 🌐 REST API

### Категории

* GET /api/categories

### Упражнения

* GET /api/exercises
* POST /api/exercises
* GET /api/exercises/{id}
* PATCH /api/exercises/{id}
* POST /api/exercises/{id}/archive
* POST /api/exercises/{id}/restore

### Подходы

* GET /api/sets
* POST /api/sets
* DELETE /api/sets/{id}

### Шаблоны

* GET /api/templates
* GET /api/templates/today
* POST /api/templates
* PATCH /api/templates/{id}
* DELETE /api/templates/{id}

### Упражнения в шаблоне

* GET /api/templates/{id}/exercises
* POST /api/templates/{id}/exercises
* PATCH /api/template-exercises/{id}
* DELETE /api/template-exercises/{id}

---

## ❌ В MVP отсутствует

* Начало/конец тренировки
* Автоматическое создание упражнений
* Сложные расписания
* Глобальные упражнения
* Аналитика
* Роли
* Админка
* Периодизация

---

## 🚀 Вектор развития

Планируемые этапы:

1. Аналитика и графики
2. Автоподстановка предыдущих весов
